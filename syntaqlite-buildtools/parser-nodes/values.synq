list ValuesRowList {
  ExprList
  fmt {
    for_each(sep: "," line) { group { "(" nest { softline child(_item) } softline ")" } }
  }
}

node ValuesClause {
  rows: index ValuesRowList

  fmt {
    group {
      "VALUES" nest { line child(rows) }
    }
  }
}
