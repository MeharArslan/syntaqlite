+++
title = "syntaqlite"
+++

A parser, formatter, validator, and language server for
[SQLite](https://sqlite.org) SQL, built on SQLite's own grammar and tokenizer.
If SQLite accepts it, syntaqlite parses it. If SQLite rejects it, so does
syntaqlite — including syntax gated behind specific versions and compile-time
flags.
