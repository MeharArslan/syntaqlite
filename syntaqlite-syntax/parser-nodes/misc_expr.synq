node Variable {
  source: inline SyntaqliteSourceSpan

  fmt { span(source) }
}

node CollateExpr {
  expr: index Expr
  collation: inline SyntaqliteSourceSpan

  fmt { child_paren_list(expr) " COLLATE " span(collation) }
}
