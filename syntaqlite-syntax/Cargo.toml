[package]
name = "syntaqlite-syntax"
version = "0.1.0"
edition = "2024"
links = "syntaqlite_syntax"

[features]
default = ["sqlite"]
sqlite = []
pin-version = ["sqlite"]
pin-cflags = ["sqlite"]
no-bundled-parser = []


[lints]
workspace = true

[build-dependencies]
cc = "1.0"
