#!/bin/bash
# Copyright 2025 The syntaqlite Authors. All rights reserved.
# Licensed under the Apache License, Version 2.0.

# Verify the codegen can regenerate all generated files from a clean state.
# See python/tools/run_bootstrap_test.py for details.

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT_DIR="$(dirname "$SCRIPT_DIR")"

exec python3 "$ROOT_DIR/python/tools/run_bootstrap_test.py" "$@"
