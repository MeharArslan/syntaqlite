[package]
name = "syntaqlite"
version = "0.1.0"
edition = "2024"

[features]
default = ["fmt"]
fmt = ["syntaqlite-runtime/fmt"]

[build-dependencies]
cc = "1.0"

[dependencies]
syntaqlite-runtime = { path = "../syntaqlite-runtime", default-features = false }
