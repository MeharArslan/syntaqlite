enum Bool { FALSE TRUE }

enum LiteralType { INTEGER FLOAT STRING BLOB NULL CURRENT QNUMBER }

enum BinaryOp {
  PLUS MINUS STAR SLASH REM
  LT GT LE GE EQ NE
  AND OR BIT_AND BIT_OR LSHIFT RSHIFT
  CONCAT PTR
}

enum UnaryOp { MINUS PLUS BIT_NOT NOT }
