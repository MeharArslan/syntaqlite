node Variable {
  source: inline SyntaqliteSourceSpan

  fmt { span(source) }
}

node CollateExpr {
  expr: index Expr
  collation: inline SyntaqliteSourceSpan

  fmt { child(expr) " COLLATE " span(collation) }
}
